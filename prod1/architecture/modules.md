# Module boundaries

- `apps/web` — UI + API surface
- `apps/worker` — background sync
- `packages/db` — schema + prisma client
- `packages/etherscan-client` — upstream API adapter
- `packages/sync-engine` — transfer ingestion logic
